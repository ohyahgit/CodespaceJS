import chalk from 'chalk'

console.log(chalk.bold.greenBright('OK!'));
try {
    tatltuae = 42;
    me = "hello";
    tatltuae = 'hi';
} catch (Error) {
    console.log(chalk.bold.redBright('Error'));
    console.log(chalk.bold.red('You cannot make a number a string!'));
}
